# תיקון טעינת רכיב Storage

האתר טען ישירות את `app.js`, ולכן `cover-storage.js` לא נטען כלל. התיקון משנה את `index.html` כך שיטען את `safe-app-loader.js`, שטוען תחילה את האפליקציה המקורית ולאחר מכן את רכיב שמירת הכריכות ב-Supabase Storage.

אין שינוי בנתוני הספרים ואין מיגרציה של כריכות קיימות.
