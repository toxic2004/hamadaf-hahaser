# בדיקה: האם עברית (Evrit) ו-Findabook באמת מייצרים הצעות (23.8.2026)

## עברית (Evrit)
- **כן, המנגנון האוטומטי רץ בפועל**: 1,260 ניסיונות בדיקה נמצאו ב-`report_checks` לאורך שבועות.
- 24 מקרים עם התאמה שנמצאה, 1 בלבד הפך להצעה שמורה ב-`price_offers`.
- 1,041 מקרים תויגו `manual_required` - **לא תקלה טכנית**: 661 כי לא אומתו יחד מחיר+קישור+מלאי, 380 כי השם הופיע רק בכותרת חיפוש בלי הוכחת רשומת מוצר. זו בדיוק ההתנהגות שהוגדרה (אין להמציא מידע חסר).
- 30 מקרים נכשלו ב-HTTP 502, כולם סביב הזמן שבו קרס ה-cron (19.8) - לא תוזמנו לניסיון חוזר מאז, כי אין cron.

### שורש בעיה טכני שנמצא ותוקן
`RENDER_SERVER_TIMEOUT_MS = 20_000` (20 שניות) ב-`supabase/functions/alerts/index.ts`, מול תיעוד מפורש ב-`render-server/DEPLOY.md`: Render Free tier "נרדם" אחרי 15 דקות חוסר פעילות, מתעורר תוך 30-60 שניות. כל בדיקה שפוגעת בשרת קר כמעט מובטחת להיכשל בטיימאאוט - לא קשור למורכבות הדף.

חשוב: הטיימאאוט של 20 שניות **קוצר בכוונה** (מתועד בקוד, 2026-08-18) כדי שבדיקת עברית איטית לא תגזול זמן מהמקורות האחרים בריצה. לא שיניתי את זה.

**התיקון**: `render-server-keep-warm` - cron job חדש שפינגס את `/health` (ציבורי, בלי סוד) כל 10 דקות, מתחת לסף ה-15 דקות שגורם להירדמות. חינמי, לא נוגע בהגנה הקיימת.

## Findabook
- כל 195 הבדיקות עדיין `pending`, 0 ניסיונות בפועל.
- **הסבר**: Findabook נרשם ב-18.8, ויום למחרת (19.8) קרס ה-cron ל-4 ימים. כל 195 הבדיקות שייכות לריצות שכבר הסתיימו/נכשלו (18-19.8), אף אחת לא שייכת לריצה פעילה.
- הסריקה עובדת לפי תור (next_attempt_at, אז created_at) - Evrit כבר היה בתור הרבה לפני שFindabook נוסף.

**מסקנה**: מוקדם מדי לשפוט. יש לבדוק שוב בעוד יום-יומיים אחרי ששני התיקונים (תזמון cron + keep-warm) פעלו מספיק זמן.
