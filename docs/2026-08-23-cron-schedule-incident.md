# תקרית: תזמון הדוחות נעלם (23.8.2026)

## מה נמצא
- `cron.job` בפרויקט Supabase נמצא **ריק לגמרי** - אין אף משימה מתוזמנת.
- אין רשומת `report_runs` כלל מ-20.8 עד 23.8 (4 ימים) - אף דוח לא נשלח אוטומטית בתקופה הזו.
- רשומת `report_runs` אחת (ערב, 19.8) נתקעה בסטטוס `running` במשך 4+ ימים, 64/455 בדיקות בלבד.
- אומת: GitHub Actions הם `workflow_dispatch` בלבד (ידני) - לא מקור תזמון חלופי. Supabase pg_cron הוא המקור היחיד.
- הגדרות ההתראה (`notification_settings`) תקינות - לא הבעיה.

## מה תוקן (בפרודקשן, ישירות, באישור מפורש)
1. הרשומה התקועה סומנה `failed`.
2. שוחזר תזמון פעמיים ביומי: `invoke-alerts-morning` (05:00 UTC) ו-`invoke-alerts-evening` (19:00 UTC) - זהה לזמנים שהניבו ריצות מוצלחות בעבר (10.8-19.8).
3. תועד ב-migration: `supabase/migrations/20260823094500_restore_twice_daily_alerts_schedule.sql`.

## לא ידוע - דורש מעקב
- **למה** cron.job התרוקן ב-19.8? לא נמצא הסבר בקוד/migrations. ייתכן: פעולה ידנית בדשבורד, שדרוג/אתחול של הפרויקט, או תקלת פלטפורמה. **מומלץ לעקוב אם זה קורה שוב** - אם כן, זו תבנית שצריכה חקירה עמוקה יותר (אולי alerting חיצוני על "אין ריצה חדשה תוך X שעות").
- שעות התזמון (05:00/19:00 UTC) לא מתחשבות ב-שעון קיץ ישראלי (IDT, UTC+3) - בקיץ הזמן המקומי בפועל הוא 08:00/22:00 ולא 07:00/21:00. זו בעיה קיימת מלפני התקרית, לא נוצרה עכשיו - צריך החלטה נפרדת אם לתקן.
